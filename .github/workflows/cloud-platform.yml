---
name: cloud-platform
on:
  push:
    branches:
      - main
    paths:
      - 'terraform/environments/cloud-platform-non-live/**'
      - '.github/workflows/cloud-platform.yml'

  pull_request:
    branches:
      - main
    paths:
      - 'terraform/environments/cloud-platform-non-live/**'
      - '.github/workflows/cloud-platform.yml'
      - '.github/workflows/cloud-platform-reusable.yml'

permissions:
  id-token: write  # This is required for requesting the JWT
  contents: read  # This is required for actions/checkout

jobs:
  terraform:
    strategy:
      fail-fast: false
      matrix:
        include:
          - application: cloud-platform-non-live
            environment: development
          - application: cloud-platform-non-live
            environment: test
          - application: cloud-platform-non-live
            environment: preproduction
          - application: cloud-platform-non-live
            environment: production
          - application: cloud-platform-live
            environment: production
    uses: ./.github/workflows/cloud-platform-reusable.yml
    with:
      application: "${{ matrix.application }}"
      environment: "${{ matrix.environment }}"
      action: "plan_apply"
    secrets:
      MODERNISATION_PLATFORM_ACCOUNT_ID: ${{ secrets.MODERNISATION_PLATFORM_ACCOUNT_ID }}
      PASSPHRASE: ${{ secrets.PASSPHRASE }}
