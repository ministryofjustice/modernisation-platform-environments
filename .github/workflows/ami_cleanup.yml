# .github/workflows/ami_cleanup.yml
name: Cloud Cleanup (AMI & EBS)

on:
  # Run previews automatically on commits to your feature branch
  push:
    branches:
      - feature-11321-identifying-snapshots-to-be-deleted

  # Optional: allow manual runs too (kept for flexibility)
  workflow_dispatch:
    inputs:
      application:
        description: 'Application folder name (e.g. example)'
        required: true
        type: string
      environments:
        description: 'Comma-separated list of environments (e.g. development,production)'
        required: true
        type: string
      cleanup_type:
        description: 'Which cleanup to run (ami, ebs, both)'
        required: true
        default: 'both'
        type: choice
        options: [ami, ebs, both]
      ami_cleanup_sh_args:
        description: 'Arguments passed to ami_cleanup.sh'
        required: false
        default: "-c -m 3"
        type: string
      ebs_age_in_months:
        description: 'Delete unattached EBS volumes older than this many months'
        required: false
        default: "1"
        type: string
      confirm_delete:
        description: 'true = perform deletion; false = preview only'
        required: true
        default: false
        type: boolean
      # Optional minute-based testing toggles (pass-through envs)
      ami_test_mode:
        description: 'Set true to enable minute-based test window for AMI script (if supported)'
        required: false
        default: false
        type: boolean
      ami_age_minutes:
        description: 'Minutes threshold for AMI test mode (if supported)'
        required: false
        default: "10"
        type: string
      ebs_test_mode:
        description: 'Set true to enable minute-based test window for EBS script (if supported)'
        required: false
        default: false
        type: boolean
      ebs_age_minutes:
        description: 'Minutes threshold for EBS test mode (if supported)'
        required: false
        default: "10"
        type: string

permissions:
  id-token: write
  contents: read

env:
  APPLICATION:  ${{ github.event.inputs.application || 'example' }}
  ENVIRONMENTS: ${{ github.event.inputs.environments || 'development' }}
  AMI_ARGS:     ${{ github.event.inputs.ami_cleanup_sh_args || '-c -m 3' }}
  EBS_MONTHS:   ${{ github.event.inputs.ebs_age_in_months || '1' }}
  # Pass-through testing envs (scripts can optionally read them)
  AMI_TEST_MODE:   ${{ github.event.inputs.ami_test_mode   || 'false' }}
  AMI_AGE_MINUTES: ${{ github.event.inputs.ami_age_minutes || '10' }}
  EBS_TEST_MODE:   ${{ github.event.inputs.ebs_test_mode   || 'false' }}
  EBS_AGE_MINUTES: ${{ github.event.inputs.ebs_age_minutes || '10' }}

jobs:
  build-matrix:
    name: Build Account Matrix
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.strategy.outputs.matrix }}
    steps:
      - name: Build strategy matrix
        id: strategy
        run: |
          set -euo pipefail
          app="${APPLICATION}"
          envs_str="${ENVIRONMENTS}"
          IFS=', ' read -r -a envs <<< "$envs_str"
          echo "Application: $app"
          echo "Environments: ${envs[@]}"
          echo '{"include":[' > matrix.json
          for env in "${envs[@]}"; do
            acc="${app}-${env}"
            echo "{\"account_name\":\"$acc\"}," >> matrix.json
          done
          sed -i '$ s/,$//' matrix.json
          echo ']}' >> matrix.json
          matrix=$(cat matrix.json)
          echo 'matrix<<EOF' >> $GITHUB_OUTPUT
          echo "${matrix}" >> $GITHUB_OUTPUT
          echo 'EOF' >> $GITHUB_OUTPUT

  fetch-secrets:
    name: Fetch ENV management secrets
    uses: ministryofjustice/modernisation-platform-github-actions/.github/workflows/aws-secrets-management.yml@4eceb2a416a197e91dc557c2ef0b5dd9afe29e0c
    secrets:
      MODERNISATION_PLATFORM_ACCOUNT_NUMBER: ${{ secrets.MODERNISATION_PLATFORM_ACCOUNT_ID }}
      PASSPHRASE: ${{ secrets.PASSPHRASE }}

  # -------------------------
  # PREVIEW (Dry-Run)
  # - Always runs on push
  # - Runs on workflow_dispatch when confirm_delete == false
  # -------------------------
  preview:
    name: Preview Deletions (Dry-Run)
    runs-on: ubuntu-latest
    needs: [build-matrix, fetch-secrets]
    if: ${{ github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && fromJSON(github.event.inputs.confirm_delete) == false) }}
    strategy:
      matrix: ${{ fromJson(needs.build-matrix.outputs.matrix) }}
      max-parallel: 1
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Ensure scripts are executable
        run: chmod +x scripts/*.sh

      - name: Decrypt Secrets (ENVIRONMENT_MANAGEMENT)
        uses: ministryofjustice/modernisation-platform-github-actions/decrypt-secrets@4eceb2a416a197e91dc557c2ef0b5dd9afe29e0c
        with:
          environment_management: ${{ needs.fetch-secrets.outputs.environment_management }}
          github_ci_user_environments_repo_pat: ${{ needs.fetch-secrets.outputs.github_ci_user_environments_repo_pat }}
          PASSPHRASE: ${{ secrets.PASSPHRASE }}

      - name: Resolve AWS Account Number
        run: |
          set -euo pipefail
          ACCOUNT_NAME="${{ matrix.account_name }}"
          ACCOUNT_NUMBER=$(jq -r -e --arg account_name "${ACCOUNT_NAME}" '.account_ids[$account_name]' <<< "$ENVIRONMENT_MANAGEMENT")
          echo "::add-mask::$ACCOUNT_NUMBER"
          echo "ACCOUNT_NUMBER=${ACCOUNT_NUMBER}" >> $GITHUB_ENV

      - name: Configure AWS Credentials (github-actions role)
        uses: aws-actions/configure-aws-credentials@e3dd6a429d7300a6a4c196c26e071d42e0343502
        with:
          role-to-assume: "arn:aws:iam::${{ env.ACCOUNT_NUMBER }}:role/github-actions"
          role-session-name: "cleanup-preview-${{ matrix.account_name }}"
          aws-region: eu-west-2

      - name: Compute effective test flags (preview)
        id: flags_preview
        run: |
          set -euo pipefail
          has_token="$(jq -r '[.commits[].message] | map(contains("[CLEANUP TEST NOW]")) | any' "$GITHUB_EVENT_PATH" 2>/dev/null || echo false)"
          head_has_token="$(jq -r '.head_commit.message | contains("[CLEANUP TEST NOW]")' "$GITHUB_EVENT_PATH" 2>/dev/null || echo false)"
          if [[ "$has_token" == "true" || "$head_has_token" == "true" || "${AMI_TEST_MODE}" == "true" ]]; then
            echo "AMI_TEST_FLAGS=--test-mode --age-minutes ${AMI_AGE_MINUTES}" >> $GITHUB_ENV
          else
            echo "AMI_TEST_FLAGS=" >> $GITHUB_ENV
          fi
          if [[ "$has_token" == "true" || "$head_has_token" == "true" || "${EBS_TEST_MODE}" == "true" ]]; then
            echo "EBS_TEST_FLAGS=--test-mode --age-minutes ${EBS_AGE_MINUTES}" >> $GITHUB_ENV
          else
            echo "EBS_TEST_FLAGS=" >> $GITHUB_ENV
          fi

      - name: AMI Preview (dry-run)
        if: ${{ github.event_name == 'push' || github.event.inputs.cleanup_type == 'ami' || github.event.inputs.cleanup_type == 'both' }}
        id: ami_preview
        run: |
          set -euo pipefail
          app="${APPLICATION}"
          args="${AMI_ARGS}"
          region="eu-west-2"

          echo "[PREVIEW] Scanning AMIs in ${region} (dry-run; no deletion)"
          echo "AMI_ARGS: ${args}"
          echo "Effective AMI test flags: ${AMI_TEST_FLAGS}"
          echo "----------------------------------------------------"

          scripts/ami_cleanup.sh -a "$app" -s ami_commands.sh $args ${AMI_TEST_FLAGS} -d delete

          TOTAL="$(grep -c '^aws ec2 deregister-image' ami_commands.sh || true)"
          [[ -f ami_candidates.csv ]] || touch ami_candidates.csv

          echo "Total deregister-image commands generated: ${TOTAL}"

          if [[ -s ami_candidates.csv ]]; then
            echo ""
            echo "Top AMI candidates (ImageId,OwnerId,CreationDate,Public,Name):"
            head -n 20 ami_candidates.csv || true
            echo ""
          else
            echo "No AMIs found matching cleanup filters."
          fi

          echo "ami_count=${TOTAL}" >> "$GITHUB_OUTPUT"
          {
            echo "### AMI Preview — ${{ matrix.account_name }}"
            echo ""
            echo "- Region: ${region}"
            echo "- Total AMIs eligible for cleanup: ${TOTAL}"
            echo "- Full candidate list and commands attached as artifacts."
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload AMI Preview Artifacts
        if: ${{ (github.event_name == 'push' || github.event.inputs.cleanup_type == 'ami' || github.event.inputs.cleanup_type == 'both') && always() }}
        uses: actions/upload-artifact@v4
        with:
          name: ami-preview-${{ matrix.account_name }}
          path: |
            ami_candidates.csv
            ami_commands.sh
          if-no-files-found: warn

      - name: EBS Preview (dry-run)
        if: ${{ github.event_name == 'push' || github.event.inputs.cleanup_type == 'ebs' || github.event.inputs.cleanup_type == 'both' }}
        id: ebs_preview
        run: |
          set -euo pipefail
          months="${EBS_MONTHS}"
          echo "[PREVIEW] Listing unattached EBS volumes older than ${months} month(s) (dry-run only; no deletion)"
          echo "Effective EBS test flags: ${EBS_TEST_FLAGS}"
          scripts/ebs_cleanup.sh ${EBS_TEST_FLAGS} -m "$months" unattached | tee ebs_candidates.txt
          EBS_COUNT="$( (grep -Eo 'vol-[0-9a-f]+' ebs_candidates.txt || true) | sort -u | wc -l | tr -d ' ' )"
          echo "Total unattached volumes matching filter: ${EBS_COUNT}"

          if [[ -s ebs_candidates.txt ]]; then
            echo ""
            echo "Top EBS candidates (volume IDs):"
            (grep -Eo 'vol-[0-9a-f]+' ebs_candidates.txt || true) | sort -u | head -n 20
            echo ""
          else
            echo "No unattached volumes found matching the filter."
          fi

          echo "ebs_count=${EBS_COUNT}" >> "$GITHUB_OUTPUT"
          [[ -f ebs_candidates.txt ]] || touch ebs_candidates.txt
          {
            echo "### EBS Preview — ${{ matrix.account_name }}"
            echo ""
            echo "- Unattached volumes slated by age filter: ${EBS_COUNT}"
            echo "- Full candidate list attached as artifact."
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload EBS Preview Artifacts
        if: ${{ (github.event_name == 'push' || github.event.inputs.cleanup_type == 'ebs' || github.event.inputs.cleanup_type == 'both') && always() }}
        uses: actions/upload-artifact@v4
        with:
          name: ebs-preview-${{ matrix.account_name }}
          path: ebs_candidates.txt
          if-no-files-found: warn

  # -------------------------
  # LIVE (Deletion) — Push tokens or manual dispatch
  #
  # Push tokens (present in ANY commit message in the push):
  #   [CLEANUP LIVE AMI]   -> AMI Delete (Live)
  #   [CLEANUP LIVE EBS]   -> EBS Delete (Live)
  #   [CLEANUP LIVE BOTH]  -> Both AMI & EBS Live
  #
  # Manual still works on workflow_dispatch with confirm_delete=true.
  # -------------------------
  ami-delete:
    name: AMI Delete (Live)
    runs-on: ubuntu-latest
    needs: [build-matrix, fetch-secrets]
    if: >
      (github.event_name == 'push' && (
        contains(github.event.head_commit.message, '[CLEANUP LIVE AMI]') ||
        contains(github.event.head_commit.message, '[CLEANUP LIVE BOTH]') ||
        contains(toJson(github.event.commits), '[CLEANUP LIVE AMI]') ||
        contains(toJson(github.event.commits), '[CLEANUP LIVE BOTH]')
      )) ||
      (github.event_name == 'workflow_dispatch' &&
        (github.event.inputs.cleanup_type == 'ami' || github.event.inputs.cleanup_type == 'both') &&
        fromJSON(github.event.inputs.confirm_delete) == true)
    strategy:
      matrix: ${{ fromJson(needs.build-matrix.outputs.matrix) }}
      max-parallel: 1
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Ensure scripts are executable
        run: chmod +x scripts/*.sh

      - name: Decrypt Secrets (ENVIRONMENT_MANAGEMENT)
        uses: ministryofjustice/modernisation-platform-github-actions/decrypt-secrets@4eceb2a416a197e91dc557c2ef0b5dd9afe29e0c
        with:
          environment_management: ${{ needs.fetch-secrets.outputs.environment_management }}
          github_ci_user_environments_repo_pat: ${{ needs.fetch-secrets.outputs.github_ci_user_environments_repo_pat }}
          PASSPHRASE: ${{ secrets.PASSPHRASE }}

      - name: Resolve AWS Account Number
        run: |
          set -euo pipefail
          ACCOUNT_NAME="${{ matrix.account_name }}"
          ACCOUNT_NUMBER=$(jq -r -e --arg account_name "${ACCOUNT_NAME}" '.account_ids[$account_name]' <<< "$ENVIRONMENT_MANAGEMENT")
          echo "::add-mask::$ACCOUNT_NUMBER"
          echo "ACCOUNT_NUMBER=${ACCOUNT_NUMBER}" >> $GITHUB_ENV

      - name: Configure AWS Credentials (github-actions role)
        uses: aws-actions/configure-aws-credentials@e3dd6a429d7300a6a4c196c26e071d42e0343502
        with:
          role-to-assume: "arn:aws:iam::${{ env.ACCOUNT_NUMBER }}:role/github-actions"
          role-session-name: "ami-delete-${{ matrix.account_name }}"
          aws-region: eu-west-2

      - name: Compute effective test flags (live AMI)
        id: flags_live_ami
        run: |
          set -euo pipefail
          has_token="$(jq -r '[.commits[].message] | map(contains("[CLEANUP TEST NOW]")) | any' "$GITHUB_EVENT_PATH" 2>/dev/null || echo false)"
          head_has_token="$(jq -r '.head_commit.message | contains("[CLEANUP TEST NOW]")' "$GITHUB_EVENT_PATH" 2>/dev/null || echo false)"
          if [[ "$has_token" == "true" || "$head_has_token" == "true" || "${AMI_TEST_MODE}" == "true" ]]; then
            echo "AMI_TEST_FLAGS=--test-mode --age-minutes ${AMI_AGE_MINUTES}" >> $GITHUB_ENV
          else
            echo "AMI_TEST_FLAGS=" >> $GITHUB_ENV
          fi

      - name: Recompute & Delete AMIs (Live)
        run: |
          set -euo pipefail
          app="${APPLICATION}"
          args="${AMI_ARGS}"
          echo "[LIVE] Collecting candidates and deregistering (if any)"
          echo "Effective AMI test flags: ${AMI_TEST_FLAGS}"
          scripts/ami_cleanup.sh -a "$app" -s ami_commands.sh $args ${AMI_TEST_FLAGS} delete
          TOTAL="$(grep -c '^aws ec2 deregister-image' ami_commands.sh || true)"
          echo "Total deregister-image commands generated (and executed): ${TOTAL}"
          if [[ -s ami_commands.sh ]]; then
            echo ""
            echo "First 20 deregistration commands:"
            grep '^aws ec2 deregister-image' ami_commands.sh | head -n 20
            echo ""
          fi
          [[ -f ami_candidates.csv ]] || touch ami_candidates.csv
          [[ -f ami_commands.sh ]] || touch ami_commands.sh

      - name: Upload AMI Live Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ami-live-${{ matrix.account_name }}
          path: |
            ami_candidates.csv
            ami_commands.sh
          if-no-files-found: warn

  ebs-delete:
    name: EBS Delete (Live)
    runs-on: ubuntu-latest
    needs: [build-matrix, fetch-secrets]
    if: >
      (github.event_name == 'push' && (
        contains(github.event.head_commit.message, '[CLEANUP LIVE EBS]') ||
        contains(github.event.head_commit.message, '[CLEANUP LIVE BOTH]') ||
        contains(toJson(github.event.commits), '[CLEANUP LIVE EBS]') ||
        contains(toJson(github.event.commits), '[CLEANUP LIVE BOTH]')
      )) ||
      (github.event_name == 'workflow_dispatch' &&
        (github.event.inputs.cleanup_type == 'ebs' || github.event.inputs.cleanup_type == 'both') &&
        fromJSON(github.event.inputs.confirm_delete) == true)
    strategy:
      matrix: ${{ fromJson(needs.build-matrix.outputs.matrix) }}
      max-parallel: 1
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Ensure scripts are executable
        run: chmod +x scripts/*.sh

      - name: Decrypt Secrets (ENVIRONMENT_MANAGEMENT)
        uses: ministryofjustice/modernisation-platform-github-actions/decrypt-secrets@4eceb2a416a197e91dc557c2ef0b5dd9afe29e0c
        with:
          environment_management: ${{ needs.fetch-secrets.outputs.environment_management }}
          github_ci_user_environments_repo_pat: ${{ needs.fetch-secrets.outputs.github_ci_user_environments_repo_pat }}
          PASSPHRASE: ${{ secrets.PASSPHRASE }}

      - name: Resolve AWS Account Number
        run: |
          set -euo pipefail
          ACCOUNT_NAME="${{ matrix.account_name }}"
          ACCOUNT_NUMBER=$(jq -r -e --arg account_name "${ACCOUNT_NAME}" '.account_ids[$account_name]' <<< "$ENVIRONMENT_MANAGEMENT")
          echo "::add-mask::$ACCOUNT_NUMBER"
          echo "ACCOUNT_NUMBER=${ACCOUNT_NUMBER}" >> $GITHUB_ENV

      - name: Configure AWS Credentials (github-actions role)
        uses: aws-actions/configure-aws-credentials@e3dd6a429d7300a6a4c196c26e071d42e0343502
        with:
          role-to-assume: "arn:aws:iam::${{ env.ACCOUNT_NUMBER }}:role/github-actions"
          role-session-name: "ebs-delete-${{ matrix.account_name }}"
          aws-region: eu-west-2

      - name: Compute effective test flags (live EBS)
        id: flags_live_ebs
        run: |
          set -euo pipefail
          has_token="$(jq -r '[.commits[].message] | map(contains("[CLEANUP TEST NOW]")) | any' "$GITHUB_EVENT_PATH" 2>/dev/null || echo false)"
          head_has_token="$(jq -r '.head_commit.message | contains("[CLEANUP TEST NOW]")' "$GITHUB_EVENT_PATH" 2>/dev/null || echo false)"
          if [[ "$has_token" == "true" || "$head_has_token" == "true" || "${EBS_TEST_MODE}" == "true" ]]; then
            echo "EBS_TEST_FLAGS=--test-mode --age-minutes ${EBS_AGE_MINUTES}" >> $GITHUB_ENV
          else
            echo "EBS_TEST_FLAGS=" >> $GITHUB_ENV
          fi

      - name: Delete EBS Volumes (Live)
        run: |
          set -euo pipefail
          months="${EBS_MONTHS}"
          echo "[LIVE] Deleting unattached EBS volumes older than ${months} month(s)"
          echo "Effective EBS test flags: ${EBS_TEST_FLAGS}"
          scripts/ebs_cleanup.sh ${EBS_TEST_FLAGS} -m "$months" delete | tee ebs_delete_log.txt
          DELETED_COUNT="$( (grep -Eo 'vol-[0-9a-f]+' ebs_delete_log.txt || true) | sort -u | wc -l | tr -d ' ' )"
          echo "Total volumes processed for deletion: ${DELETED_COUNT}"
          if [[ -s ebs_delete_log.txt ]]; then
            echo ""
            echo "First 20 volume IDs processed:"
            (grep -Eo 'vol-[0-9a-f]+' ebs_delete_log.txt || true) | sort -u | head -n 20
            echo ""
          fi
          [[ -f ebs_delete_log.txt ]] || touch ebs_delete_log.txt

      - name: Upload EBS Live Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ebs-live-${{ matrix.account_name }}
          path: ebs_delete_log.txt
