locals {
  environment               = var.environment
  resource_application_name = var.resource_application_name
  resource_name_prefix      = var.resource_application_name
  service_configuration     = var.service_configuration
  tags                      = var.tags
}
