scripts:
%{ for sid in oracle_sids ~}
#${length(sid)} / ${sid} / ${length(sid)-2}
  - name: oracle_health_check_${sid}
    script: sudo -i -u oracle /opt/script-exporter/oracle-health-${sid}.sh
%{ endfor ~}