[project]
name = "integration-hub-file-transfer-schema-validation"
version = "1.0.0"
description = "Schema validation for Integration Hub File Transfer configuration"
requires-python = ">=3.13"
dependencies = [
    "check-jsonschema==0.37.4",
]