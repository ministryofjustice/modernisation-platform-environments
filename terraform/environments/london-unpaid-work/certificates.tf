# Placeholder ALB certificate configuration.
#
# The legacy web and API ALBs shared the public SSL certificate ARN from core-vpc
# (`strategic_public_ssl_arn`).
# Update `alb.tf` with the certificate ARN once it is available.