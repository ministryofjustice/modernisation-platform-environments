---
schemaVersion: "2.2"
description: "SSM Document for configuring an RDS app"
mainSteps:
  - name: DeployRDS
    action: aws:runPowerShellScript
    precondition:
      StringEquals:
        - platformType
        - Windows
    inputs:
    runCommand:
      - |