# ---------------------------------------------------------------------------------------------------------------------
# LOCALS
# ---------------------------------------------------------------------------------------------------------------------
locals {
  deploy_to = ["development"]
  geofence_app = {
    jar_filename = "flink-moj-geofence-1.0.0.jar"
  }

  rules_app = {
    jar_filename = "flink-rules-1.0.0.jar"
  }
}
