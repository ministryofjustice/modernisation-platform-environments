#--Transfer Service Webapp
/* resource "awscc_transfer_web_app" "this" {
  identity_provider_details = {
    instance_arn = var.aws_identity_centre_store_arn
    role         = aws_iam_role.transfer.arn
  }
} */
