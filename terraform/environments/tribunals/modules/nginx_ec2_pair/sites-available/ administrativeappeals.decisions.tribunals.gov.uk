server {
    listen 80;
    listen [::]:80;
    server_name administrativeappeals.decisions.tribunals.gov.uk;
    
    # Redirect all HTTP traffic to HTTPS
    return 301 https://$server_name$request_uri;
}