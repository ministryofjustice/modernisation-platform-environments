# LinOTP URL — both containers share the same network namespace in the ECS task
URL=http://localhost/validate/simplecheck

# LinOTP realm (configured in LinOTP admin UI)
REALM=laa-workspaces

# Debug mode
Debug=True

# SSL verification (disabled — using HTTP internally)
SSL_CHECK=False
